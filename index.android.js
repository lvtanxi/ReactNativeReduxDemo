/**
 * @flow
 */

import React from 'react';
import {AppRegistry} from 'react-native';
import AppContainer from './src/containers/AppContainer';
AppRegistry.registerComponent('ReactNativeReduxDemo', () => AppContainer);
